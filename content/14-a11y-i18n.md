---
id: 14-a11y-i18n
title: 无障碍与国际化
order: 14
icon: ♿
description: WCAG、语义化、焦点管理、ARIA、Vue I18n、Intl、RTL 与多语言工程实践。
---

## wcag-semantic

title: 可访问性的起点是语义化，而不是补 ARIA
followups: [wcag-semantic-followup-1]
links: [04-css/native-popover-dialog-anchor, a11y-quick-wins-basic, aria-roles-states]
difficulty: 基础
tags: [a11y, 语义化]

### 一句话

语义元素自带角色、键盘行为和可访问树信息；用按钮就用 <button>，不要拿 <div> 模拟点击；表单关联 label、标题层级、列表结构、表格语义都是基础。

### 题目

为什么说“先用对 HTML 元素，再谈 ARIA”？请举几个典型例子。

### 答案要点

- 语义元素自带角色、键盘行为和可访问树信息
- 用按钮就用 `<button>`，不要拿 `<div>` 模拟点击
- 表单关联 `label`、标题层级、列表结构、表格语义都是基础
- ARIA 主要用于补充原生语义不足，而不是替代原生语义

### 代码示例

```html
<!-- ❌ 反例：div 模拟按钮，丢失键盘可访问性 -->
<div class="btn" onclick="submit()">提交</div>

<!-- ✅ 用原生 button -->
<button type="button" @click="submit">提交</button>

<!-- ✅ 表单：label 关联 input，错误用 aria-describedby -->
<label for="email">邮箱</label>
<input
  id="email"
  v-model="email"
  type="email"
  required
  aria-invalid="true"
  aria-describedby="email-err"
/>
<p id="email-err" class="error">请填写有效邮箱</p>

<!-- ✅ 标题层级：h1→h2→h3 不要跳级 -->
<main>
  <h1>页面标题</h1>
  <section>
    <h2>章节标题</h2>
    <h3>子标题</h3>
  </section>
</main>

<!-- ✅ 图片必须有替代文本（装饰性图片用空 alt） -->
<img src="logo.svg" alt="公司 Logo" />
<img src="bg.svg" alt="" role="presentation" />

<!-- ✅ 列表用 ul/ol/li，表格用 table/th/td/caption -->
<table>
  <caption>
    2026 销售数据
  </caption>
  <thead>
    <tr>
      <th scope="col">月份</th>
      <th scope="col">销量</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th scope="row">1月</th>
      <td>120</td>
    </tr>
  </tbody>
</table>
```

### 追问

- 「可访问性的起点是语义化，而不是补 ARIA」在真实项目里最容易踩到哪些边界条件？
- 你会用哪些测试、日志或指标证明这个方案有效？
- 如果需求规模、团队成本或兼容性要求变化，你会如何调整方案？

### 常见误区

- 回答「可访问性的起点是语义化，而不是补 ARIA」时不要停在定义层，要补充适用条件、失效边界和验证方式。
- 如果没有补充输入边界、失败路径和替代方案，答案会停留在“知道概念”的层面。
- 如果不说明监控指标、发布策略和回滚方式，工程可信度会明显不足。
- 相关标签是 a11y、语义化，回答时要补充可验证手段：如何复现问题、用什么指标判断有效、出现异常时如何降级或回滚。

### 延伸

- 很多无障碍问题不是"没加 aria"，而是先天组件语义就错了

## focus-keyboard

title: 键盘导航与焦点管理是前端最常见的无障碍硬指标
followups: [focus-keyboard-followup-1]
links: [04-css/native-popover-dialog-anchor, a11y-quick-wins-basic, aria-roles-states]
difficulty: 进阶
tags: [焦点管理, 键盘]

### 一句话

打开弹窗时应把焦点移入，关闭时归还触发元素；模态框内要做焦点圈闭，避免 Tab 跑到背景页面；键盘操作应支持 Esc 关闭、方向键切换、Enter/Space 激活等。

### 题目

做弹窗、抽屉、下拉菜单时，焦点管理为什么经常决定可访问性成败？

### 答案要点

- 打开弹窗时应把焦点**移入**到首个可交互元素，关闭时**归还**触发它的按钮
- 模态框内要做**焦点圈闭（focus trap）**，避免 Tab 跑到背景页面
- 键盘操作应支持 **Esc 关闭、方向键切换、Enter/Space 激活、Tab 顺序合理**
- 隐藏元素用 `inert` 或 `aria-hidden="true"`，避免读屏器读到背景内容
- 自定义组件（dropdown / tabs）必须遵循 [WAI-ARIA Authoring Practices](https://www.w3.org/WAI/ARIA/apg/) 的键盘交互模式
- 测试：用键盘从头到尾走一遍流程，再用 NVDA / VoiceOver 听一遍

### 代码示例

```vue
<script setup lang="ts">
import { ref, watch, onUnmounted } from 'vue';

const props = defineProps<{ open: boolean }>();
const emit = defineEmits<{ (e: 'close'): void }>();

const dialogRef = ref<HTMLDivElement>();
let lastActive: HTMLElement | null = null;

watch(
  () => props.open,
  async (open) => {
    if (open) {
      lastActive = document.activeElement as HTMLElement;
      await nextTick();
      // 1. 焦点移入弹窗第一个可聚焦元素
      const first = dialogRef.value?.querySelector<HTMLElement>(
        'input,button,select,textarea,[tabindex]:not([tabindex="-1"])',
      );
      first?.focus();
    } else {
      // 2. 关闭后焦点归还触发元素
      lastActive?.focus();
    }
  },
);

// 3. 焦点圈闭：Tab/Shift+Tab 不能跳出弹窗
function onKeydown(e: KeyboardEvent) {
  if (!props.open) return;
  if (e.key === 'Escape') return emit('close');
  if (e.key !== 'Tab') return;

  const focusables = dialogRef.value!.querySelectorAll<HTMLElement>(
    'input,button,select,textarea,a[href],[tabindex]:not([tabindex="-1"])',
  );
  const first = focusables[0];
  const last = focusables[focusables.length - 1];

  if (e.shiftKey && document.activeElement === first) {
    e.preventDefault();
    last.focus();
  } else if (!e.shiftKey && document.activeElement === last) {
    e.preventDefault();
    first.focus();
  }
}

window.addEventListener('keydown', onKeydown);
onUnmounted(() => window.removeEventListener('keydown', onKeydown));
</script>

<template>
  <Teleport to="body">
    <div v-if="open" ref="dialogRef" role="dialog" aria-modal="true" aria-labelledby="dialog-title">
      <h2 id="dialog-title">确认操作</h2>
      <button @click="$emit('close')" aria-label="关闭对话框">×</button>
      <slot />
    </div>
  </Teleport>
</template>
```

### 追问

- 「键盘导航与焦点管理是前端最常见的无障碍硬指标」在真实项目里最容易踩到哪些边界条件？
- 你会用哪些测试、日志或指标证明这个方案有效？
- 如果需求规模、团队成本或兼容性要求变化，你会如何调整方案？

### 常见误区

- 回答「键盘导航与焦点管理是前端最常见的无障碍硬指标」时不要停在定义层，要补充适用条件、失效边界和验证方式。
- 如果没有补充输入边界、失败路径和替代方案，答案会停留在“知道概念”的层面。
- 如果不说明监控指标、发布策略和回滚方式，工程可信度会明显不足。
- 相关标签是 焦点管理、键盘，回答时要补充可验证手段：如何复现问题、用什么指标判断有效、出现异常时如何降级或回滚。

### 延伸

- 焦点顺序混乱不仅影响残障用户，也会显著影响键盘重度用户效率

## aria-live-errors

title: aria-live、错误提示与动态内容更新
followups: [aria-live-errors-followup-1]
difficulty: 进阶
tags: [ARIA, 表单]

### 一句话

对重要动态消息使用 aria-live 区域；表单错误要和具体字段建立关联，如 aria-describedby；加载中、成功、失败状态要有可感知文案，而不只是颜色变化。

### 题目

动态提示、表单报错、加载状态要怎样做，屏幕阅读器才能感知？

### 答案要点

- 对重要动态消息使用 `aria-live` 区域
- 表单错误要和具体字段建立关联，如 `aria-describedby`
- 加载中、成功、失败状态要有可感知文案，而不只是颜色变化

### 代码示例

```vue
<template>
  <!-- 1. 重要状态变更：aria-live="assertive" 立即播报 -->
  <div role="alert" aria-live="assertive">
    <p v-if="error">{{ error }}</p>
  </div>

  <!-- 2. 一般动态信息：aria-live="polite" 等待空闲再播 -->
  <div aria-live="polite" class="sr-only">
    {{ statusMessage }}
  </div>

  <!-- 3. 加载中：aria-busy + 可见文字 -->
  <button :aria-busy="loading" :disabled="loading">
    <span v-if="loading">提交中...</span>
    <span v-else>提交</span>
  </button>

  <!-- 4. 表单错误：aria-describedby 关联 -->
  <label for="pwd">密码</label>
  <input
    id="pwd"
    type="password"
    :aria-invalid="!!errors.pwd"
    aria-describedby="pwd-help pwd-err"
  />
  <p id="pwd-help">至少 8 位，包含字母与数字</p>
  <p id="pwd-err" v-if="errors.pwd" class="error">{{ errors.pwd }}</p>
</template>

<style>
/* 屏幕阅读器可见但视觉隐藏 */
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}
</style>
```

### 追问

- 「aria-live、错误提示与动态内容更新」在真实项目里最容易踩到哪些边界条件？
- 你会用哪些测试、日志或指标证明这个方案有效？
- 如果需求规模、团队成本或兼容性要求变化，你会如何调整方案？

### 常见误区

- 回答「aria-live、错误提示与动态内容更新」时不要停在定义层，要补充适用条件、失效边界和验证方式。
- 如果没有补充输入边界、失败路径和替代方案，答案会停留在“知道概念”的层面。
- 如果不说明监控指标、发布策略和回滚方式，工程可信度会明显不足。
- 相关标签是 ARIA、表单，回答时要补充可验证手段：如何复现问题、用什么指标判断有效、出现异常时如何降级或回滚。

### 延伸

- 可访问性不是只给"最终结果"，而是让交互过程全程可理解

## reduced-motion-contrast

title: 对比度、高对比模式与 reduced motion
followups: [reduced-motion-contrast-followup-1]
difficulty: 基础
tags: [视觉可访问性, 动效]

### 一句话

对比度不足会导致正文、按钮和状态信息难以辨认；不能只靠颜色表达状态，需要图标、文本或结构辅助；对大动效、滚动动画要尊重 prefers-reduced-motion。

### 题目

为什么“看起来高级”的设计，可能恰恰会损害可访问性？

### 答案要点

- 对比度不足会导致正文、按钮和状态信息难以辨认
- 不能只靠颜色表达状态，需要图标、文本或结构辅助
- 对大动效、滚动动画要尊重 `prefers-reduced-motion`

### 代码示例

```css
/* 1. 对比度：正文至少 4.5:1，大字至少 3:1（WCAG AA） */
:root {
  --text: #111; /* 与白底对比度 ~17:1 */
  --text-muted: #666; /* 与白底对比度 ~5.7:1 */
  --link: #0958d9; /* 与白底对比度 ~4.6:1 */
}

/* 2. 不仅靠颜色：状态用颜色 + 图标 + 文字 */
.status-error::before {
  content: '❌ ';
}
.status-success::before {
  content: '✅ ';
}

/* 3. 高对比度模式 */
@media (prefers-contrast: more) {
  :root {
    --text: #000;
    --bg: #fff;
    --border: #000;
  }
  button {
    border: 2px solid currentColor;
  }
}

/* 4. 减少动效（眩晕症友好） */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}

/* 5. focus-visible：仅键盘导航显示焦点环 */
button:focus {
  outline: none;
}
button:focus-visible {
  outline: 3px solid #0ea5e9;
  outline-offset: 2px;
}
```

```ts
// JS 端检测用户偏好
const reduceMotion = matchMedia('(prefers-reduced-motion: reduce)').matches;
if (!reduceMotion) playEntryAnimation();
```

### 追问

- 「对比度、高对比模式与 reduced motion」在真实项目里最容易踩到哪些边界条件？
- 你会用哪些测试、日志或指标证明这个方案有效？
- 如果需求规模、团队成本或兼容性要求变化，你会如何调整方案？

### 常见误区

- 回答「对比度、高对比模式与 reduced motion」时不要停在定义层，要补充适用条件、失效边界和验证方式。
- 如果没有补充输入边界、失败路径和替代方案，答案会停留在“知道概念”的层面。
- 如果不说明监控指标、发布策略和回滚方式，工程可信度会明显不足。
- 相关标签是 视觉可访问性、动效，回答时要补充可验证手段：如何复现问题、用什么指标判断有效、出现异常时如何降级或回滚。

### 延伸

- 无障碍不是限制设计，而是让设计在更多人群中可用

## vue-i18n-intl

title: Vue I18n、Intl API 与多语言资源组织
followups: [vue-i18n-intl-followup-1]
difficulty: 进阶
tags: [i18n, Intl]

### 一句话

文案资源需要命名规范、按页面/领域拆分、支持懒加载；日期、数字、货币、相对时间应尽量使用 Intl 系列 API；复数规则、占位顺序、语序变化不能硬编码中文思维。

### 题目

国际化为什么不只是“把文案抽出去”？日期、数字、复数规则又该如何处理？

### 答案要点

- 文案资源需要命名规范、按页面/领域拆分、支持懒加载
- 日期、数字、货币、相对时间应尽量使用 `Intl` 系列 API
- 复数规则、占位顺序、语序变化不能硬编码中文思维

### 代码示例

```ts
// 1. Vue I18n 9：按语言/页面拆分 + 懒加载
import { createI18n } from 'vue-i18n';

const i18n = createI18n({
  legacy: false,
  locale: 'zh-CN',
  fallbackLocale: 'en',
  messages: {},
});

async function loadLocale(locale: string) {
  const messages = await import(`@/locales/${locale}.json`);
  i18n.global.setLocaleMessage(locale, messages.default);
  i18n.global.locale.value = locale as any;
  document.documentElement.lang = locale;
}
```

```vue
<template>
  <!-- 2. 模板里使用 -->
  <h1>{{ $t('dashboard.title') }}</h1>

  <!-- 3. 复数（注意不同语言的规则不同）-->
  <p>{{ $t('cart.items', { count }, count) }}</p>

  <!-- 4. 占位符顺序由翻译者控制 -->
  <p>{{ $t('greeting', { name: user.name, date }) }}</p>
</template>
```

```json
// locales/zh-CN.json
{
  "dashboard": { "title": "仪表盘" },
  "cart": {
    "items": "购物车有 {count} 件商品 | 购物车有 {count} 件商品"
  },
  "greeting": "你好 {name}，今天是 {date}"
}

// locales/en.json
{
  "cart": {
    "items": "no items | one item | {count} items"
  },
  "greeting": "Hello {name}, today is {date}"
}
```

```ts
// 5. Intl API：日期/数字/货币（无需第三方库）
new Intl.DateTimeFormat('zh-CN', { dateStyle: 'long' }).format(new Date());
// '2026年5月6日'

new Intl.NumberFormat('en-US', { style: 'currency', currency: 'USD' }).format(1234.5);
// '$1,234.50'

new Intl.RelativeTimeFormat('zh-CN', { numeric: 'auto' }).format(-3, 'day');
// '3天前'

new Intl.PluralRules('en').select(1); // 'one'
new Intl.PluralRules('en').select(2); // 'other'

// 列表格式化
new Intl.ListFormat('zh-CN', { type: 'conjunction' }).format(['苹果', '香蕉', '橘子']);
// '苹果、香蕉和橘子'
```

### 追问

- 在 Vue 项目里落地「Vue I18n、Intl API 与多语言资源组织」时，响应式边界和组件更新时机要注意什么？
- 如果这个方案导致状态耦合或调试困难，你会怎么拆分和验证？
- 它和常见替代方案相比，适合什么团队规模和业务复杂度？

### 常见误区

- 回答「Vue I18n、Intl API 与多语言资源组织」时如果只罗列 API，不解释响应式或组件更新链路，深挖时会露出断层。
- 只背 API 名称，不解释响应式依赖收集、组件更新边界和生命周期时序。
- 把所有状态都塞进同一个 store 或 composable，忽略作用域、释放时机和可测试性。
- 相关标签是 i18n、Intl，回答时要补充可验证手段：如何复现问题、用什么指标判断有效、出现异常时如何降级或回滚。

### 延伸

- i18n 的难点常在工程组织和内容流程，不只是技术 API

## rtl-ssr

title: RTL、逻辑属性与 SSR 下的国际化一致性
followups: [rtl-ssr-followup-1]
difficulty: 进阶
tags: [RTL, SSR]

### 一句话

RTL 语言下布局方向会反转，margin-inline-start、padding-inline-end 等逻辑属性更稳；SSR 要保证服务端和客户端语言环境一致，否则会出现 hydration mismatch。

### 题目

为什么国际化项目应尽量使用逻辑属性而不是 left/right？SSR 时语言切换又有哪些额外问题？

### 答案要点

- RTL 语言下布局方向会反转，`margin-inline-start`、`padding-inline-end` 等逻辑属性更稳
- SSR 要保证服务端和客户端语言环境一致，否则会出现 hydration mismatch
- 字体回退、行高、断词策略在不同语言中也要重新审视

### 代码示例

```css
/* ✅ 用逻辑属性，自动适配 LTR/RTL */
.card {
  margin-inline-start: 16px; /* LTR=margin-left, RTL=margin-right */
  margin-inline-end: 16px;
  padding-inline: 24px;
  border-inline-start: 2px solid;
  text-align: start; /* 替代 left/right */
}

/* ✅ 仅在 RTL 下生效的样式 */
[dir='rtl'] .icon-arrow {
  transform: scaleX(-1); /* 箭头镜像 */
}

/* ❌ 反例：硬编码方向 */
.bad {
  margin-left: 16px;
  text-align: left;
}
```

```ts
// HTML 根节点设置 lang + dir，浏览器和 CSS 都会响应
function applyLocale(locale: string) {
  const rtlLocales = ['ar', 'he', 'fa', 'ur'];
  const isRtl = rtlLocales.some((l) => locale.startsWith(l));
  document.documentElement.lang = locale;
  document.documentElement.dir = isRtl ? 'rtl' : 'ltr';
}
```

```ts
// SSR：从请求头/cookie 检测语言，确保首屏一致
// Nuxt server middleware
export default defineEventHandler((event) => {
  const accept = getHeader(event, 'accept-language') || '';
  const cookieLang = getCookie(event, 'lang');
  const lang = cookieLang || accept.split(',')[0].split('-')[0] || 'en';

  // 注入到 SSR context，让 Vue I18n 在服务端用同一份消息
  event.context.lang = lang;
});

// Vue 应用入口
const lang = useNuxtApp().ssrContext?.event.context.lang ?? 'en';
i18n.global.locale.value = lang;
```

### 追问

- 「RTL、逻辑属性与 SSR 下的国际化一致性」在真实项目里最容易踩到哪些边界条件？
- 你会用哪些测试、日志或指标证明这个方案有效？
- 如果需求规模、团队成本或兼容性要求变化，你会如何调整方案？

### 常见误区

- 回答「RTL、逻辑属性与 SSR 下的国际化一致性」时不要停在定义层，要补充适用条件、失效边界和验证方式。
- 如果没有补充输入边界、失败路径和替代方案，答案会停留在“知道概念”的层面。
- 如果不说明监控指标、发布策略和回滚方式，工程可信度会明显不足。
- 相关标签是 RTL、SSR，回答时要补充可验证手段：如何复现问题、用什么指标判断有效、出现异常时如何降级或回滚。

### 延伸

- 只支持英文和中文时感觉不到问题，多语言一扩张，布局假设就会暴露

## screen-reader-testing

title: 屏幕阅读器调试与回归
followups: [screen-reader-testing-followup-1]
difficulty: 进阶
tags: [屏幕阅读器, 测试]

### 一句话

系统：macOS VoiceOver（Cmd+F5）、Windows NVDA / JAWS、iOS VoiceOver、Android TalkBack。

### 题目

怎么在开发期就保证组件对屏幕阅读器友好？有哪些工具和习惯？

### 答案要点

- 系统：macOS VoiceOver（Cmd+F5）、Windows NVDA / JAWS、iOS VoiceOver、Android TalkBack
- 单测：`@testing-library/jest-dom` + `axe-core`（如 `@axe-core/playwright`）跑 a11y 规则
- 浏览器：Chrome DevTools 的 Accessibility Tree 面板查看实际朗读结构
- 常见问题：图标按钮缺 aria-label、表单控件缺 label、动态内容缺 aria-live
- 焦点：Modal / Drawer 打开时 focus trap，关闭时焦点回到触发元素
- 自动化：CI 跑 axe，把违反 WCAG 2.1 AA 的规则标红 / 阻塞合并

### 代码示例

```ts
import { test, expect } from '@playwright/test';
import AxeBuilder from '@axe-core/playwright';

test('首页无可访问性问题', async ({ page }) => {
  await page.goto('/');
  const results = await new AxeBuilder({ page }).withTags(['wcag2a', 'wcag2aa']).analyze();
  expect(results.violations).toEqual([]);
});
```

```vue
<button :aria-label="收藏 ? '取消收藏' : '收藏'" :aria-pressed="收藏" @click="toggleStar">
  <StarIcon :filled="收藏" />
</button>

<div role="status" aria-live="polite" class="sr-only">
  {{ status === 'saving' ? '正在保存…' : status === 'saved' ? '已保存' : '' }}
</div>
```

### 追问

- 针对「屏幕阅读器调试与回归」，你会优先补哪些边界用例和回归用例？
- 如何避免测试过度耦合实现细节，导致重构时大量误报？
- 这类测试在 CI 中如何分层运行，兼顾速度和信心？

### 常见误区

- 回答「屏幕阅读器调试与回归」时如果只写 happy path，不说明边界和稳定性，测试价值会被高估。
- 只覆盖 happy path，不覆盖边界输入、异常路径、异步时序和回归用例。
- 测试过度依赖实现细节，重构后大量误报，反而降低团队维护意愿。
- 相关标签是 屏幕阅读器、测试，回答时要补充可验证手段：如何复现问题、用什么指标判断有效、出现异常时如何降级或回滚。

### 延伸

- 自动化检查只能覆盖一部分规则类问题，关键流程仍必须人工跑一遍键盘和屏幕阅读器
- 设计稿评审阶段就引入 a11y checklist，通常比上线后返工成本低得多

## intl-format-deep

title: Intl 标准 API 在国际化中的实际应用
followups: [intl-format-deep-followup-1]
difficulty: 进阶
tags: [Intl, 国际化]

### 一句话

Intl.NumberFormat：千分位、货币、紧凑（1.2K）、单位（米/秒）；Intl.DateTimeFormat：根据 locale 输出日期，自动处理农历 / 阿拉伯日历；Intl.RelativeTimeFormat："3 小时前"。

### 题目

浏览器内置的 `Intl.*` 在 i18n 中能解决哪些问题？哪些坑要注意？

### 答案要点

- `Intl.NumberFormat`：千分位、货币、紧凑（1.2K）、单位（米/秒）
- `Intl.DateTimeFormat`：根据 locale 输出日期，自动处理农历 / 阿拉伯日历
- `Intl.RelativeTimeFormat`："3 小时前"，比手写数组干净
- `Intl.PluralRules`：根据数字给出复数类（zero / one / few / many / other），写英文复数必备
- `Intl.ListFormat`：把数组转成自然语言列表（"A、B 和 C"）
- `Intl.Segmenter`：按字 / 词 / 句切分，取代旧的正则切词
- 注意：旧浏览器对 locale 数据支持不全，必要时 polyfill `@formatjs/*`

### 代码示例

```ts
const cny = new Intl.NumberFormat('zh-CN', { style: 'currency', currency: 'CNY' });
console.log(cny.format(12345.6)); // ¥12,345.60

const rtf = new Intl.RelativeTimeFormat('zh-CN', { numeric: 'auto' });
console.log(rtf.format(-1, 'day')); // 昨天
console.log(rtf.format(-3, 'day')); // 3天前

const pr = new Intl.PluralRules('en');
function pluralize(n: number) {
  const rule = pr.select(n);
  return { one: 'item', other: 'items' }[rule] ?? 'items';
}

const list = new Intl.ListFormat('zh-CN', { type: 'conjunction' });
console.log(list.format(['前端', '后端', '运维'])); // 前端、后端和运维

const seg = new Intl.Segmenter('zh-CN', { granularity: 'word' });
for (const s of seg.segment('我爱前端开发')) console.log(s.segment, s.isWordLike);
```

### 追问

- 「Intl 标准 API 在国际化中的实际应用」在真实项目里最容易踩到哪些边界条件？
- 你会用哪些测试、日志或指标证明这个方案有效？
- 如果需求规模、团队成本或兼容性要求变化，你会如何调整方案？

### 常见误区

- 回答「Intl 标准 API 在国际化中的实际应用」时不要停在定义层，要补充适用条件、失效边界和验证方式。
- 如果没有补充输入边界、失败路径和替代方案，答案会停留在“知道概念”的层面。
- 如果不说明监控指标、发布策略和回滚方式，工程可信度会明显不足。
- 相关标签是 Intl、国际化，回答时要补充可验证手段：如何复现问题、用什么指标判断有效、出现异常时如何降级或回滚。

### 延伸

- 复杂消息（嵌套占位符 / 选择器）继续用 ICU MessageFormat（@formatjs/intl-messageformat）
- 服务端要预先按 Accept-Language 选择 locale，避免客户端首屏闪烁

## aria-roles-states

title: ARIA 的 role / state / property 是什么，什么时候该用
followups: [aria-roles-states-followup-1]
links: [04-css/native-popover-dialog-anchor, a11y-quick-wins-basic, focus-keyboard]
difficulty: 进阶
tags: [a11y, ARIA]

### 一句话

能用语义 HTML（`<button>` `<nav>` `<main>`）就别用 ARIA；ARIA 是给"自定义组件"补语义的，第一原则是 **No ARIA is better than Bad ARIA**。

### 题目

ARIA 的 role / aria-\* 属性分别解决什么问题？典型组件（Tabs / Modal / Combobox）需要哪些标注？

### 答案要点

- `role`：告诉辅助技术"这是一个什么角色"（button / dialog / tab / menu / alert）
- `aria-label / aria-labelledby / aria-describedby`：补充无法从可见文本推断的标签
- `aria-expanded / aria-selected / aria-checked / aria-pressed`：状态
- `aria-controls / aria-owns`：建立逻辑上的归属关系
- `aria-live="polite|assertive"`：告诉读屏器"这块内容会动态更新，请播报"
- 第一原则："No ARIA is better than Bad ARIA"——能用 `<button>` `<dialog>` 就别用 div + role
- 典型组件：
  - Modal：`role="dialog"` + `aria-modal="true"` + 焦点陷阱 + `aria-labelledby`
  - Tabs：tablist > tab[aria-selected] + tabpanel[aria-labelledby]
  - Combobox：`role="combobox"` + `aria-expanded` + `aria-controls` + `aria-activedescendant`

### 代码示例

```html
<button type="button" aria-expanded="false" aria-controls="menu1">菜单</button>
<ul id="menu1" role="menu" hidden>
  <li role="menuitem">选项 1</li>
</ul>

<div role="dialog" aria-modal="true" aria-labelledby="title1">
  <h2 id="title1">确认删除？</h2>
</div>
```

### 追问

- 「ARIA 的 role / state / property 是什么，什么时候该用」在真实项目里最容易踩到哪些边界条件？
- 你会用哪些测试、日志或指标证明这个方案有效？
- 如果需求规模、团队成本或兼容性要求变化，你会如何调整方案？

### 常见误区

- 回答「ARIA 的 role / state / property 是什么，什么时候该用」时不要停在定义层，要补充适用条件、失效边界和验证方式。
- 如果没有补充输入边界、失败路径和替代方案，答案会停留在“知道概念”的层面。
- 如果不说明监控指标、发布策略和回滚方式，工程可信度会明显不足。
- 相关标签是 a11y、ARIA，回答时要补充可验证手段：如何复现问题、用什么指标判断有效、出现异常时如何降级或回滚。

### 延伸

- Chrome DevTools 的 Accessibility 面板可查看节点的可访问性树
- WAI-ARIA Authoring Practices 给出了所有常见组件的 ARIA 模式参考
- 测试：Axe / Lighthouse / VoiceOver / NVDA

## i18n-locale-formatter

title: 国际化中数字、日期、复数怎么处理才对
followups: [i18n-locale-formatter-followup-1]
difficulty: 进阶
tags: [i18n, Intl]

### 一句话

别再自己拼字符串。用浏览器内建 `Intl.NumberFormat / DateTimeFormat / RelativeTimeFormat / PluralRules`，零依赖处理 200+ 语言。

### 题目

请展示 `Intl` API 在数字、日期、相对时间和复数处理中的用法。

### 答案要点

- `Intl.NumberFormat(locale, { style: 'currency', currency: 'CNY' })` —— 货币、千分位、单位
- `Intl.DateTimeFormat(locale, options)` —— 日期/时间格式（含中文长格式、农历可选）
- `Intl.RelativeTimeFormat(locale)` —— "3 天前" "in 5 minutes"
- `Intl.PluralRules(locale)` —— 处理复数变体（英语 one/other，俄语 one/few/many/other）
- `Intl.ListFormat / Segmenter / Collator` —— 列表拼接、分词、排序
- 文案管理：`vue-i18n` / `react-intl` / `formatjs` 用 ICU MessageFormat 表达复数与变量

### 代码示例

```js
new Intl.NumberFormat('zh-CN', { style: 'currency', currency: 'CNY' }).format(1234567.89);

new Intl.DateTimeFormat('zh-CN', { dateStyle: 'long', timeStyle: 'short' }).format(new Date());

const rtf = new Intl.RelativeTimeFormat('zh-CN', { numeric: 'auto' });
rtf.format(-3, 'day'); // "3 天前"

const pr = new Intl.PluralRules('en');
pr.select(1); // "one"
pr.select(5); // "other"

const seg = new Intl.Segmenter('zh', { granularity: 'word' });
[...seg.segment('我爱前端')].map((s) => s.segment);
```

### 追问

- 「国际化中数字、日期、复数怎么处理才对」在真实项目里最容易踩到哪些边界条件？
- 你会用哪些测试、日志或指标证明这个方案有效？
- 如果需求规模、团队成本或兼容性要求变化，你会如何调整方案？

### 常见误区

- 回答「国际化中数字、日期、复数怎么处理才对」时不要停在定义层，要补充适用条件、失效边界和验证方式。
- 如果没有补充输入边界、失败路径和替代方案，答案会停留在“知道概念”的层面。
- 如果不说明监控指标、发布策略和回滚方式，工程可信度会明显不足。
- 相关标签是 i18n、Intl，回答时要补充可验证手段：如何复现问题、用什么指标判断有效、出现异常时如何降级或回滚。

### 延伸

- 时区处理首选 `Intl.DateTimeFormat` 的 `timeZone` 选项 + Temporal（提案中）
- 真实业务中翻译文件优先 ICU MessageFormat，避免拼接 bug
- RTL（阿拉伯语）需要 `dir="rtl"` 与 `:dir(rtl)` 选择器配合

## form-accessibility

title: 表单 a11y 完整 checklist
followups: [form-accessibility-followup-1]
links: [10-architecture/component-library, a11y-quick-wins-basic, aria-roles-states]
difficulty: 进阶
tags: [无障碍, 表单, 高频]

### 一句话

**label 必须显式关联 input**（`<label for>` 或包裹）；**错误用 aria-invalid + aria-describedby** 关联到错误文案；原生表单优先用 `required`，自定义控件再补 `aria-required`；提交后焦点回到第一个错误项；屏幕阅读器要能完整朗读"标签 + 当前值 + 错误"。

### 题目

做一个登录表单 / 注册表单怎么保证无障碍？提交校验失败时怎么处理才友好？

### 答案要点

- **基础语义**
  - 用原生 `<form>` `<input>` `<button>`，不要 `<div onclick>` 伪装
  - 每个 input 一个明确的 label，要么 `<label for="email">` 要么把 input 包在 label 里
  - placeholder 不能替代 label：不同读屏器行为不一致，且用户开始输入后提示消失，可见性和可访问名称都不稳定
- **必填 / 类型 / 自动填充**
  - 必填：原生 input 优先用 `required`；自定义控件或复杂组合控件再补 `aria-required="true"`
  - 类型语义化：`type="email" / tel / url / password`，移动端弹合适键盘
  - autocomplete：`autocomplete="email" / current-password / new-password / one-time-code`，省事+安全
- **错误处理**
  - 出错的 input 加 `aria-invalid="true"`
  - 错误文案用唯一 id，input 的 `aria-describedby` 指过去
  - 错误文案也用 `role="alert"` 或在 live region 里更新（屏幕阅读器立刻读出）
  - 提交后用 JS 把焦点移到第一个错误项（用户不用 Tab 找）
- **不仅靠颜色**
  - 红框 + 图标 + 文字三重提示（色盲也能识别）
  - 对比度 ≥ 4.5:1
- **键盘**
  - Tab 顺序符合视觉顺序
  - Enter 提交（除非显式禁用）
  - Esc 关闭弹窗 / 清空（视产品而定）
- **加载与禁用**
  - 提交中：button disabled + 显示 spinner + `aria-busy="true"`
  - 不要禁用整个 form 让用户以为卡死，加文案"提交中..."

### 代码示例

```html
<form @submit.prevent="onSubmit" novalidate>
  <div class="field">
    <label for="email">邮箱</label>
    <input
      id="email"
      v-model="form.email"
      type="email"
      autocomplete="email"
      required
      aria-required="true"
      :aria-invalid="errors.email ? 'true' : 'false'"
      :aria-describedby="errors.email ? 'email-err' : undefined"
    />
    <p v-if="errors.email" id="email-err" class="err" role="alert">{{ errors.email }}</p>
  </div>

  <button type="submit" :disabled="submitting" :aria-busy="submitting">
    {{ submitting ? '提交中…' : '登录' }}
  </button>
</form>

<script setup>
  async function onSubmit() {
    errors.value = validate(form.value);
    await nextTick();
    const first = document.querySelector('[aria-invalid="true"]');
    (first as HTMLElement | null)?.focus();
  }
</script>

<style>
  .field input[aria-invalid='true'] {
    border-color: #d4380d;
    outline-color: #d4380d;
  }
  .err::before {
    content: '⚠ ';
  }
</style>
```

### 追问

- 「表单 a11y 完整 checklist」在真实项目里最容易踩到哪些边界条件？
- 你会用哪些测试、日志或指标证明这个方案有效？
- 如果需求规模、团队成本或兼容性要求变化，你会如何调整方案？

### 常见误区

- 回答「表单 a11y 完整 checklist」时不要停在定义层，要补充适用条件、失效边界和验证方式。
- 如果没有补充输入边界、失败路径和替代方案，答案会停留在“知道概念”的层面。
- 如果不说明监控指标、发布策略和回滚方式，工程可信度会明显不足。
- 相关标签是 无障碍、表单、高频，回答时要补充可验证手段：如何复现问题、用什么指标判断有效、出现异常时如何降级或回滚。

### 延伸

- 自动填充友好：`name` 用标准词（given-name / family-name / postal-code）
- 二次确认密码用 `autocomplete="new-password"` 让浏览器不带旧密码

## a11y-quick-wins-basic

title: 前端无障碍（a11y）最容易拿分的 5 件事
followups: [a11y-quick-wins-basic-followup-1]
links: [04-css/native-popover-dialog-anchor, aria-roles-states, focus-keyboard]
difficulty: 基础
tags: [a11y, 无障碍, 基础]

### 一句话

语义化标签 + 可见焦点 + 替代文本 + 充足对比度 + 表单 label —— 这五件事覆盖大部分 a11y 检查项。

### 题目

列出 5 个前端最容易实现、收益最大的无障碍最佳实践。

### 答案要点

1. **语义化标签**：用 `<button>` 不要 `<div onClick>`；导航用 `<nav>`，主体用 `<main>`，有助屏幕阅读器导航
2. **可见焦点**：不要 `outline: none` 一刀切；可换成 `:focus-visible` 自定义样式
3. **图片 alt**：装饰图 `alt=""`（明确空值），有信息的图描述用途；icon 按钮加 `aria-label`
4. **颜色对比度**：正文 ≥ 4.5:1，大字 ≥ 3:1；不要用纯色区分错误（要 + 文字 / icon）
5. **表单 label**：`<label for="email">` 或 `<label><input/></label>`，否则屏幕阅读器读不到字段名

### 代码示例

```html
<nav>
  <ul>
    <li><a href="/">首页</a></li>
  </ul>
</nav>

<form>
  <label for="email">邮箱</label>
  <input id="email" type="email" required aria-describedby="email-help" />
  <p id="email-help">用于接收订单通知</p>
</form>

<button aria-label="关闭弹窗" class="icon-btn">×</button>

<style>
  :focus-visible {
    outline: 2px solid #0ea5e9;
    outline-offset: 2px;
  }
</style>
```

### 常见误区

- 给 div 加 `role="button"` 但没加 `tabIndex` 和键盘事件 → 不能被键盘聚焦
- placeholder 当 label 用 → 输入后看不见提示，对屏幕阅读器也不友好
- 错误用红色文案不加 icon / 文字 → 色盲用户感知不到

### 追问

- aria-live 区分 polite vs assertive
- 怎么测 a11y（axe-core / Lighthouse / 真机 VoiceOver）
- 跳转链接（skip to content）放在哪里

### 延伸

- WCAG 2.2 新增了 7 条标准，可关注 focus 不被遮挡和拖拽替代
- iOS VoiceOver / macOS VoiceOver 可在浏览器开调试

## i18n-async-locale-routing

title: 多语言资源拆分与按路由懒加载
followups: [i18n-async-locale-routing-followup-1, i18n-async-locale-routing-followup-2, i18n-async-locale-routing-followup-3]
difficulty: 资深
tags: [i18n, 异步, vue-i18n, 高频]

### 一句话

按"语言 × 模块"拆分语言包成独立 chunk；启动只加载当前 locale 的公共部分，路由切换时按需 import 对应模块包；切换语言时把已加载模块重新拉一份目标语言；vue-i18n 9 提供 lazy load 和 setLocaleMessage API，自己实现也很轻。

### 题目

你们 i18n 多语言资源是怎么拆分和运行时切换的？切路由时异步语言包加载时机是怎么处理的？资源加载完全依赖 vue-i18n 还是有自己的策略？

### 答案要点

**拆分维度**

- 按 **locale**（zh-CN / en-US / ja-JP）肯定拆
- 再按 **模块 / 路由**（home / order / settings）二级拆
- 命名：`locales/{locale}/{module}.json`
- 公共部分：common / errors / global，启动时拉

**加载策略（关键）**

- **app start**：只加载 `${currentLocale}/common.json`
- **路由切换**：在 `router.beforeEach` 拦截，拉 `${currentLocale}/${routeMeta.i18nNs}.json`，加载完才 next()
- **language switch**：拉所有"已加载过的模块"的目标语言版本，setLocaleMessage 注入；UI 整体重新渲染（compute property reactive）
- **预取**：在空闲时（rIC）prefetch 用户可能下一步用到的模块语言包

**实现选择**

- vue-i18n 9.x 已经支持 `legacy: false`（Composition API 模式）+ `setLocaleMessage(locale, msg)` 动态注入
- 自家实现也不复杂：维护 `loadedNs: Set<string>` 防重复，用 `import.meta.glob` 动态 import
- 也可以用 i18next + i18next-http-backend，按需远程拉

**避免的坑**

- 切语言时**只切了 i18n** 但 dayjs / number formatter 没切：要联动
- 路由 await 加载语言包卡住 UX：要有 fallback message + skeleton
- 语言包过大：超大 enum 文案应分页 / 按业务路径懒加载
- SSR 必须一开始就加载所有 server 渲染的 namespace

### 代码示例

```ts
import { createI18n } from 'vue-i18n';

export const i18n = createI18n({
  legacy: false,
  locale: 'zh-CN',
  fallbackLocale: 'en-US',
  messages: {},
});

const localeModules = import.meta.glob('../locales/**/*.json');

const loaded = new Set<string>();

export async function loadNs(locale: string, ns: string) {
  const key = `${locale}:${ns}`;
  if (loaded.has(key)) return;
  const path = `../locales/${locale}/${ns}.json`;
  const loader = localeModules[path];
  if (!loader) return;
  const mod = (await loader()) as { default: Record<string, unknown> };
  i18n.global.mergeLocaleMessage(locale, { [ns]: mod.default });
  loaded.add(key);
}

export async function switchLocale(target: string) {
  const reload = Array.from(loaded)
    .map((k) => k.split(':'))
    .filter(([loc]) => loc !== target)
    .map(([, ns]) => loadNs(target, ns));
  await Promise.all(reload);
  i18n.global.locale.value = target;
  document.documentElement.lang = target;
}

router.beforeEach(async (to) => {
  const ns = to.meta?.i18nNs as string | undefined;
  if (ns) await loadNs(i18n.global.locale.value, ns);
});
```

### 常见误区

- 把所有语言一次全 import：bundle 翻 N 倍
- 路由切换时不 await 语言包：用户先看到 key（如 `home.title`）
- 只换 i18n locale 不换 html lang：a11y / 字体回退异常
- 切换语言时清空已加载缓存：用户来回切，反复请求

### 追问

- vue-i18n 还是自己写，怎么选？
  - 简单业务用 vue-i18n；复杂格式（plural / context）/ 多端复用考虑 i18next + ICU MessageFormat
- 切路由时语言包加载失败怎么办？
  - fallback locale + UI 容错（不阻塞渲染，先用 key + 后台重试）
- 服务端渲染时怎么协调？
  - SSR 一开始注入页面用到的全部 ns，client hydrate 时已包含；客户端切换继续懒加载

### 延伸

- 进阶：把语言包接入 CDN + ETag，浏览器/CDN 双缓存
- 工程：CI 跑一致性脚本，确保 zh / en / ja 之间 key 一致；缺失 key 自动报警

## wcag-semantic-followup-1

title: 追问：「可访问性的起点是语义化，而不是补 ARIA」在真实项目里最容易踩到哪些边界条件
difficulty: 基础
tags: [a11y, 语义化, 追问]
parent: wcag-semantic

### 题目

如果面试官追问：「可访问性的起点是语义化，而不是补 ARIA」在真实项目里最容易踩到哪些边界条件？

### 答案要点

#### 核心回答

- 先界定「可访问性的起点是语义化，而不是补 ARIA」的适用条件，再说明哪些输入、运行环境或团队约束会让默认方案失效。
- 围绕核心机制展开：正常路径要能讲清楚，异常路径要说明降级、替代方案和用户可见影响。
- 原题中的关键点「语义元素自带角色、键盘行为和可访问树信息」要进一步补到边界条件里，而不是只复述结论。

## focus-keyboard-followup-1

title: 追问：「键盘导航与焦点管理是前端最常见的无障碍硬指标」在真实项目里最容易踩到哪些边界条件
difficulty: 进阶
tags: [焦点管理, 键盘, 追问]
parent: focus-keyboard

### 题目

如果面试官追问：「键盘导航与焦点管理是前端最常见的无障碍硬指标」在真实项目里最容易踩到哪些边界条件？

### 答案要点

#### 核心回答

- 验证要从可复现样例开始：准备正向、边界和失败用例，确认「键盘导航与焦点管理是前端最常见的无障碍硬指标」不是只在理想输入下成立。
- 再补可观测指标：核心机制应该能通过日志、耗时、错误率、命中率、覆盖率或用户行为指标观察到变化。
- 如果指标没有改善，要能回到原题机制定位原因，而不是继续堆配置或换工具。

## aria-live-errors-followup-1

title: 追问：「aria-live、错误提示与动态内容更新」在真实项目里最容易踩到哪些边界条件
difficulty: 进阶
tags: [ARIA, 表单, 追问]
parent: aria-live-errors

### 题目

如果面试官追问：「aria-live、错误提示与动态内容更新」在真实项目里最容易踩到哪些边界条件？

### 答案要点

#### 核心回答

- 先界定「aria-live、错误提示与动态内容更新」的适用条件，再说明哪些输入、运行环境或团队约束会让默认方案失效。
- 围绕核心机制展开：正常路径要能讲清楚，异常路径要说明降级、替代方案和用户可见影响。
- 原题中的关键点「对重要动态消息使用 aria-live 区域」要进一步补到边界条件里，而不是只复述结论。

## reduced-motion-contrast-followup-1

title: 追问：「对比度、高对比模式与 reduced motion」在真实项目里最容易踩到哪些边界条件
difficulty: 基础
tags: [视觉可访问性, 动效, 追问]
parent: reduced-motion-contrast

### 题目

如果面试官追问：「对比度、高对比模式与 reduced motion」在真实项目里最容易踩到哪些边界条件？

### 答案要点

#### 核心回答

- 先界定「对比度、高对比模式与 reduced motion」的适用条件，再说明哪些输入、运行环境或团队约束会让默认方案失效。
- 围绕核心机制展开：正常路径要能讲清楚，异常路径要说明降级、替代方案和用户可见影响。
- 原题中的关键点「对比度不足会导致正文、按钮和状态信息难以辨认」要进一步补到边界条件里，而不是只复述结论。

## vue-i18n-intl-followup-1

title: 追问：在 Vue 项目里落地「Vue I18n、Intl API 与多语言资源组织」时，响应式边界和组件更新时机要注意什么
difficulty: 进阶
tags: [i18n, Intl, 追问]
parent: vue-i18n-intl

### 题目

如果面试官追问：在 Vue 项目里落地「Vue I18n、Intl API 与多语言资源组织」时，响应式边界和组件更新时机要注意什么？

### 答案要点

#### 核心回答

- 落地时先收敛改动面：选低风险页面或模块试点，保留旧路径作为对照。
- 迁移计划要说明数据兼容、配置开关、监控指标和失败后的恢复路径。
- 团队推进重点不是一次性重写，而是把「Vue I18n、Intl API 与多语言资源组织」拆成可验证的小步骤，逐步替换高风险部分。

## rtl-ssr-followup-1

title: 追问：「RTL、逻辑属性与 SSR 下的国际化一致性」在真实项目里最容易踩到哪些边界条件
difficulty: 进阶
tags: [RTL, SSR, 追问]
parent: rtl-ssr

### 题目

如果面试官追问：「RTL、逻辑属性与 SSR 下的国际化一致性」在真实项目里最容易踩到哪些边界条件？

### 答案要点

#### 核心回答

- 先界定「RTL、逻辑属性与 SSR 下的国际化一致性」的适用条件，再说明哪些输入、运行环境或团队约束会让默认方案失效。
- 围绕核心机制展开：正常路径要能讲清楚，异常路径要说明降级、替代方案和用户可见影响。
- 原题中的关键点「RTL 语言下布局方向会反转，margin-inline-start、padding-inline-end 等逻辑属性更稳」要进一步补到边界条件里，而不是只复述结论。

## screen-reader-testing-followup-1

title: 追问：针对「屏幕阅读器调试与回归」，你会优先补哪些边界用例和回归用例
difficulty: 进阶
tags: [屏幕阅读器, 测试, 追问]
parent: screen-reader-testing

### 题目

如果面试官追问：针对「屏幕阅读器调试与回归」，你会优先补哪些边界用例和回归用例？

### 答案要点

#### 核心回答

- 先界定「屏幕阅读器调试与回归」的适用条件，再说明哪些输入、运行环境或团队约束会让默认方案失效。
- 结合 回归信心 展开：正常路径要能讲清楚，异常路径要说明降级、替代方案和用户可见影响。
- 原题中的关键点「系统：macOS VoiceOver（Cmd+F5）、Windows NVDA / JAWS、iOS VoiceOver、Android TalkBack」要进一步补到边界条件里，而不是只复述结论。

## intl-format-deep-followup-1

title: 追问：「Intl 标准 API 在国际化中的实际应用」在真实项目里最容易踩到哪些边界条件
difficulty: 进阶
tags: [Intl, 国际化, 追问]
parent: intl-format-deep

### 题目

如果面试官追问：「Intl 标准 API 在国际化中的实际应用」在真实项目里最容易踩到哪些边界条件？

### 答案要点

#### 核心回答

- 先界定「Intl 标准 API 在国际化中的实际应用」的适用条件，再说明哪些输入、运行环境或团队约束会让默认方案失效。
- 围绕核心机制展开：正常路径要能讲清楚，异常路径要说明降级、替代方案和用户可见影响。
- 原题中的关键点「Intl.NumberFormat：千分位、货币、紧凑（1.2K）、单位（米/秒）」要进一步补到边界条件里，而不是只复述结论。

## aria-roles-states-followup-1

title: 追问：「ARIA 的 role / state / property 是什么，什么时候该用」在真实项目里最容易踩到哪些边界条件
difficulty: 进阶
tags: [a11y, ARIA, 追问]
parent: aria-roles-states

### 题目

如果面试官追问：「ARIA 的 role / state / property 是什么，什么时候该用」在真实项目里最容易踩到哪些边界条件？

### 答案要点

#### 核心回答

- 先界定「ARIA 的 role / state / property 是什么，什么时候该用」的适用条件，再说明哪些输入、运行环境或团队约束会让默认方案失效。
- 围绕核心机制展开：正常路径要能讲清楚，异常路径要说明降级、替代方案和用户可见影响。
- 原题中的关键点「role：告诉辅助技术"这是一个什么角色"（button / dialog / tab / menu / alert）」要进一步补到边界条件里，而不是只复述结论。

## i18n-locale-formatter-followup-1

title: 追问：「国际化中数字、日期、复数怎么处理才对」在真实项目里最容易踩到哪些边界条件
difficulty: 进阶
tags: [i18n, Intl, 追问]
parent: i18n-locale-formatter

### 题目

如果面试官追问：「国际化中数字、日期、复数怎么处理才对」在真实项目里最容易踩到哪些边界条件？

### 答案要点

#### 核心回答

- 先界定「国际化中数字、日期、复数怎么处理才对」的适用条件，再说明哪些输入、运行环境或团队约束会让默认方案失效。
- 围绕核心机制展开：正常路径要能讲清楚，异常路径要说明降级、替代方案和用户可见影响。
- 原题中的关键点「Intl.NumberFormat(locale, { style: 'currency', currency: 'CNY' }) —— 货币、千分位、单位」要进一步补到边界条件里，而不是只复述结论。

## form-accessibility-followup-1

title: 追问：「表单 a11y 完整 checklist」在真实项目里最容易踩到哪些边界条件
difficulty: 进阶
tags: [无障碍, 表单, 高频, 追问]
parent: form-accessibility

### 题目

如果面试官追问：「表单 a11y 完整 checklist」在真实项目里最容易踩到哪些边界条件？

### 答案要点

#### 核心回答

- 先界定「表单 a11y 完整 checklist」的适用条件，再说明哪些输入、运行环境或团队约束会让默认方案失效。
- 围绕核心机制展开：正常路径要能讲清楚，异常路径要说明降级、替代方案和用户可见影响。
- 原题中的关键点「每个 input 一个明确的 label，要么 要么把 input 包在 label 里」要进一步补到边界条件里，而不是只复述结论。

## a11y-quick-wins-basic-followup-1

title: 追问：「前端无障碍（a11y）最容易拿分的 5 件事」在真实项目里最容易踩到哪些边界条件
difficulty: 基础
tags: [a11y, 无障碍, 基础, 追问]
parent: a11y-quick-wins-basic

### 题目

如果面试官追问：「前端无障碍（a11y）最容易拿分的 5 件事」在真实项目里最容易踩到哪些边界条件？

### 答案要点

#### 核心回答

- 先界定「前端无障碍（a11y）最容易拿分的 5 件事」的适用条件，再说明哪些输入、运行环境或团队约束会让默认方案失效。
- 围绕核心机制展开：正常路径要能讲清楚，异常路径要说明降级、替代方案和用户可见影响。
- 原题中的关键点「1. 语义化标签：用 不要 ；导航用 ，主体用 ，有助屏幕阅读器导航」要进一步补到边界条件里，而不是只复述结论。

## i18n-async-locale-routing-followup-1

title: 追问：在 Vue 项目里落地「多语言资源拆分与按路由懒加载」时，响应式边界和组件更新时机要注意什么
difficulty: 资深
tags: [i18n, 异步, vue-i18n, 高频, 追问]
parent: i18n-async-locale-routing

### 题目

如果面试官追问：在 Vue 项目里落地「多语言资源拆分与按路由懒加载」时，响应式边界和组件更新时机要注意什么？

### 答案要点

#### 核心回答

- 落地时先收敛改动面：选低风险页面或模块试点，保留旧路径作为对照。
- 迁移计划要说明数据兼容、配置开关、监控指标和失败后的恢复路径。
- 团队推进重点不是一次性重写，而是把「多语言资源拆分与按路由懒加载」拆成可验证的小步骤，逐步替换高风险部分。

## i18n-async-locale-routing-followup-2

title: 追问：如果这个方案导致状态耦合或调试困难，你会怎么拆分和验证
difficulty: 资深
tags: [i18n, 异步, vue-i18n, 高频, 追问]
parent: i18n-async-locale-routing

### 题目

如果面试官追问：如果这个方案导致状态耦合或调试困难，你会怎么拆分和验证？

### 答案要点

#### 核心回答

- 验证要从可复现样例开始：准备正向、边界和失败用例，确认「多语言资源拆分与按路由懒加载」不是只在理想输入下成立。
- 再补可观测指标：响应式与组件边界应该能通过日志、耗时、错误率、命中率、覆盖率或用户行为指标观察到变化。
- 如果指标没有改善，要能回到原题机制定位原因，而不是继续堆配置或换工具。

## i18n-async-locale-routing-followup-3

title: 追问：它和常见替代方案相比，适合什么团队规模和业务复杂度
difficulty: 资深
tags: [i18n, 异步, vue-i18n, 高频, 追问]
parent: i18n-async-locale-routing

### 题目

如果面试官追问：它和常见替代方案相比，适合什么团队规模和业务复杂度？

### 答案要点

#### 核心回答

- 落地时先收敛改动面：选低风险页面或模块试点，保留旧路径作为对照。
- 迁移计划要说明数据兼容、配置开关、监控指标和失败后的恢复路径。
- 团队推进重点不是一次性重写，而是把「多语言资源拆分与按路由懒加载」拆成可验证的小步骤，逐步替换高风险部分。
